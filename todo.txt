TODO:
# check if we should remove dependencies to django early?
# tesseract errors should be transformed to our own errors
# get the tesseract error messages back
# replace img in the server with links to the actual img
# get the img's url in the database
# generer la database pour de vrai
# parser la database
# en cherchant un keyword, juste afficher le nom de comics correspondant
# create a simple binary to query the database
# store a precomputed SQL databse for the comics
# find a way to check if the venv has been activated from the check.sh script. If it hasn't, prompt the user to do it
and exit
